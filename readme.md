# TypeScript + Webpack Base Project

- Install libraries using `npm install`
- Launch development server and watch for changes using `npm start`
- New bundle entry points can be added to **webpack.config.js**